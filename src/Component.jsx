






/*
render a component that returns a div with the text given as "info"
* @param {string} info -  text to render as information
* @returns {html} html with the text
*/
function Component({info}) {
  return (
    <div>{info}</div>
  )
}

export default Component